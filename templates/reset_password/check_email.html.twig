{% extends 'base.html.twig' %}

{% block title %}Email de Réinitialisation du Mot de Passe Envoyé{% endblock %}

{% block body %}
    <div class="min-h-screen bg-blue-50 flex items-center justify-center">
        <div class="bg-white max-w-md mx-auto p-8 border border-gray-200 rounded-lg shadow">
            <p class="text-gray-700 mb-4">
                Si un compte correspondant à votre email existe, un email vient d'être envoyé contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
                Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </p>
            <p class="text-gray-700">
                Si vous ne recevez pas d'email, veuillez vérifier votre dossier spam ou 
                <a href="{{ path('app_forgot_password_request') }}" class="text-blue-600 hover:text-blue-800 underline">réessayer</a>.
            </p>
        </div>
    </div>
{% endblock %}
